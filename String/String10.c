//Take a sentence from the user and find longest word init//
#include <stdio.h>
#include <string.h>
int main(){
    char sen[300],larw[30],word[30];
    int i,ml=0,cl=0;
    printf("Enter a Sentence: ");
    gets(sen);
    cl = strlen(sen);
    return 0;
}
