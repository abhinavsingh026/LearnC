//This program is to check Stream length Function//
#include <stdio.h>
#include <string.h>
int main(){
    char str[] = "Welcome to India";
    int len;
    len = strlen(str);
    printf("Total no. of Characters: %d",strlen(str));
    printf("\nLength of the String is: %d",len);
    return 0;
}
