#include <stdio.h>
int main(){
    int n1,n2;
    printf("Enter two natural no.s ");
    scanf("%d%d",&n1,&n2);
    printf("No.s before being Swapped are: %d %d\n",n1,n2);
    printf("No.s after being Swapped are %d %d",n2,n1);
    return 0;
}
